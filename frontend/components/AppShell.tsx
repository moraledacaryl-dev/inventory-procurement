import Link from "next/link";
const links=["dashboard","items","suppliers","locations","stock","purchasing","receiving","counts","reports","integrations"];
export function AppShell({title,children}:{title:string;children:React.ReactNode}){return <div className="shell"><aside className="sidebar"><div className="brand">Hidden Oasis<br/>Inventory & Procurement</div><nav className="nav">{links.map(x=><Link key={x} href={`/${x}`}>{x[0].toUpperCase()+x.slice(1)}</Link>)}</nav></aside><main className="content"><div className="topline"><div><div className="eyebrow">Operations system</div><h1>{title}</h1></div><span className="badge">Pass 6 Operational</span></div>{children}</main></div>}
