"use client";
import {FormEvent,useEffect,useState} from "react";import {AppShell} from "../../components/AppShell";import {DataTable} from "../../components/DataTable";import {api} from "../../lib/api";
type Location={id:string;code:string;name:string;location_type:string;is_active:boolean};export default function Page(){const[rows,setRows]=useState<Location[]>([]),[msg,setMsg]=useState("");async function load(){try{setRows(await api("/locations"))}catch(e){setMsg((e as Error).message)}}useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api("/locations",{method:"POST",body:JSON.stringify({code:f.get("code"),name:f.get("name"),location_type:f.get("type")})});e.currentTarget.reset();load()}catch(x){setMsg((x as Error).message)}}return <AppShell title="Locations"><section className="card"><h2>Add storage location</h2><form className="inline-form" onSubmit={submit}><input name="code" placeholder="Code" required/><input name="name" placeholder="Location name" required/><select name="type"><option value="storeroom">Storeroom</option><option value="department">Department</option><option value="warehouse">Warehouse</option></select><button className="primary compact">Add location</button></form>{msg&&<p className="status">{msg}</p>}</section><section className="card section-gap"><h2>Active locations</h2><DataTable columns={["Code","Name","Type","Status"]} rows={rows.map(x=>[x.code,x.name,x.location_type,x.is_active?"Active":"Inactive"])}/></section></AppShell>}
