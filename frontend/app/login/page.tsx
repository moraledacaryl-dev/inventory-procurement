"use client";
import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {API} from "../../lib/api";
export default function Login(){const router=useRouter();const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget);const r=await fetch(`${API}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});const b=await r.json();if(!r.ok){setError(b.detail||"Unable to sign in");return}localStorage.setItem("inventory_token",b.access_token);router.push("/dashboard")}return <main className="login"><section className="login-card"><div className="eyebrow">Hidden Oasis</div><h1>Inventory & Procurement</h1><p>Sign in to manage stock and purchasing.</p><form onSubmit={submit}><label className="field">Email<input type="email" name="email" required/></label><label className="field">Password<input type="password" name="password" required/></label><button className="primary">Sign in</button></form>{error&&<div className="notice">{error}</div>}</section></main>}
